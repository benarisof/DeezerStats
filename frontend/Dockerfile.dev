FROM node:22-alpine

WORKDIR /app

# Couche cachée séparément : ne se réinvalide que si package*.json change
COPY package*.json ./
RUN npm install

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
