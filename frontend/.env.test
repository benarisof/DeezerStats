# Chargé automatiquement par Vitest (mode "test" par défaut) : évite que les tests échouent faute
# de VITE_API_URL. Valeur factice, sans conséquence — les tests qui font de vraies requêtes mockent
# `fetch` (voir shared/api/httpClient.test.ts) ou le module `@/shared/config/env` directement.
VITE_API_URL=http://localhost:5231/api/v1
